const apiUrl = '/api/v1'
const greetingUrl = 'https://main.d177anvw131z2v.amplifyapp.com/'
const maxFileSize = 20971520
const acceptedFileImage = ".jpeg, .jpg, .png"
const acceptedFileAudio = "audio/mpeg"
const wsSourceUrl = '/request'

const config = {
    apiUrl,
    greetingUrl,
    maxFileSize,
    acceptedFileImage,
    acceptedFileAudio,
    wsSourceUrl
}

export default config